```bash

# install dependencies
$ npm install

# serve with hot reload at localhost:3000 and run DB - server
$ npm run dev
$ npm run start-server

# build for production and launch server
$ npm run build
$ npm run start

# generate static project
$ npm run generate

```
